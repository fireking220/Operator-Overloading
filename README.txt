This program will build a binary search tree where each node has a linear linked list of people with contacts. In this program we will overload different operators including =,+,+=,==,!=,<,<=,>,>=,<<, and >>